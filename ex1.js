// Crie uma classe FiguraGeometrica com os atributos base e altura

class FiguraGeometrica {
  constructor(base, altura) {
    this.base = base;
    this.altura = altura;
  }
}

module.exports = FiguraGeometrica;

